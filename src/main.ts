import { createApp } from "vue";
import "./style.css";
import App from "./App.vue";
import OpenSeadragon from "openseadragon";

// window.OpenSeadragon = (options: any): Function => {
//   return OpenSeadragon(options);
// };

createApp(App).mount("#app");
